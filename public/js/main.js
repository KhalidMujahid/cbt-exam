const numberQuestions = document.querySelector("#number_questions");
const generateQuestions = document.querySelector("#generate_question");

generateQuestions?.addEventListener("click", (e) => {
  e.preventDefault();
});
